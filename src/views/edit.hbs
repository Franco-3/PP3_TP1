<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    <form action="/users/edit/{{usuario.idUsuario}}" method="POST">
                        <div class="form-group m-1">
                            <input type="text" value="{{usuario.Nombre}}" class="form-control" name="name" placeholder="Nombre" autofocus>
                        </div>
                        <div class="form-group m-1">
                            <input type="email" value="{{usuario.Correo}}" class="form-control" name="mail" placeholder="Correo electrónico">
                        </div>

                        <div class="input-group mb-3">
                            <span class="input-group-text"><i class="fas fa-lock"></i></span>
                            <input class="form-control" id="password" name="password" placeholder="Contraseña" value="{{usuario.Contraseña}}">
                            <span class="input-group-text">
                                <i class="far fa-eye" id="togglePassword" 
                            style="cursor: pointer"></i>
                            </span>
                        </div>

                        <div class="form-group m-1">
                            <button class="btn btn-success btn-block m-1">
                                Guardar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
